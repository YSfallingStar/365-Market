import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#2998f6',
    },
    secondary: {
      main: '#ff93dd',
    },
    neutral: {
      main: '#64748B',
      contrastText: '#fff',
    },
  },
});